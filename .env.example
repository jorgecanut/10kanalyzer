# Copy to .env next to docker-compose.yml (Dockge loads this automatically).

# Host port the container publishes. Point your nginx at it.
HTTP_PORT=8000

# The SEC requires a real contact (name + email) for the requests you make.
EDGAR_IDENTITY=Your Name you@example.com
